export enum ERoutes {
  products = "/products",
  products_stats = "/products/stats",
  orders = "/orders",
}

export enum ERoutesDetail {
  product = ":productId",
  order = ":orderId",
  login = "/login",
}
